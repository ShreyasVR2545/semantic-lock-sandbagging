# environments
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/

# model weights & caches (anchored to the repo root: a bare `models/` pattern also
# matches src/models/, which silently excluded the entire model code package)
/models/
/checkpoints/
*.safetensors
*.bin
*.pt
*.ckpt
hf_cache/
.cache/

# raw experiment dumps (aggregated CSVs ARE committed)
results/**/raw/
results/**/*.jsonl
results/**/adapters/

# archives
*.zip

# os / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
.scratchpath
